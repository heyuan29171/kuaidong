/* =========================================================
 * 排行榜 Token 配置（本文件已被 .gitignore 排除，不会上传到仓库）
 * 把 GitHub fine-grained token 填到下面的 token 里。
 * 复制 js/leaderboard-config.example.js 为 js/leaderboard-config.js
 * 后按此填写即可。token 留空 = 排行榜功能禁用（不影响游玩）。
 * ========================================================= */
window.LEADERBOARD_CONFIG = {
  token: "",
};